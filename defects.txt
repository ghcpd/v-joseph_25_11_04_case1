1. README.md (Usage Example): Running the provided sample fails with `ModuleNotFoundError: No module named 'datasync'` because the project does not ship any packaging metadata and the package is not installed in the active environment. Executing `python readme_example.py` reproduced the failure on 2025-11-04.
2. README.md (Usage Example, API Reference) vs sync.py (line 3): The class initializer uses the keyword `chunk_size` and defaults to `chunk_size=100`, `retry_limit=MAX_RETRIES` (10). Documentation claims the parameter is named `batch_size`, defaults to 50, and must be between 10 and 100.
3. README.md (API Reference) vs sync.py (lines 13-18): `SyncManager.connect` requires an `auth_token` argument and defaults `timeout` to `2` (int). README omits `auth_token`, declares `timeout` defaults to `1.5`, and describes it as a float.
4. README.md (Usage Example/API Reference) vs sync.py (lines 20-27): `push` expects the keyword `mode` with allowed values `'sync'` or `'async'`. Documentation shows `async_mode: bool = True` and calls `manager.push(..., async_mode=True)`, which raises `TypeError`.
5. README.md (Usage Example) vs sync.py (lines 8-11 & 29-31): The class exposes `get_synced_count()`; there is no `synced_count` attribute, so `manager.synced_count` raises `AttributeError`.
6. README.md (API Reference) vs sync.py (lines 14-18): The doc promises a `ConnectionError` "if unreachable", but the implementation only validates the URL scheme and returns `True` without touching the network.
7. Package layout vs sync.py (line 1): `sync.py` imports `datasync.config`, yet the repository does not contain a `datasync/` package directory. Importing `sync` directly (`python -c "import sync"`) fails with `ModuleNotFoundError`. The documentation assumes `datasync` is importable.
